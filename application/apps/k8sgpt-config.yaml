apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8sgpt-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "16"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/yourorg/production-homelab.git
    targetRevision: main
    path: application/values/k8sgpt-config
  destination:
    server: https://kubernetes.default.svc
    namespace: k8sgpt
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
